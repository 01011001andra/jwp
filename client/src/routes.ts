//  public routes
export const publicRoutes = ["/", "/login", "/register"];

// admin routes
export const adminRoutes = ["/buku", "/peminjam"];

// user routes
export const userRoutes = ["/list-buku", "/pinjam"];

// auth prefix untuk next api auth
export const apiAuthPrefix = "/api/auth";
